# patterns/

Conventions to FOLLOW when writing code. Prescriptive: truth is by decision, so a mismatch between a
pattern doc and the code is usually a code problem to flag, not a doc to rewrite. Every seat reads the
floor (`vertical-slice-anatomy`, `backend-patterns`, `frontend-patterns`, `testing`, `codegen`)
before it writes a line; a doc marked `status=draft` was scanned by `init`, not decided — promote it as
you confirm each section.

A README in this folder is never a queue entry.
