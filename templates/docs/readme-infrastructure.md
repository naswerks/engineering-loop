# infrastructure/

What runs under the hood — the technologies and plumbing. Descriptive: the code is truth, and these docs
are verified against source (`docs-audit-feature` fixes the doc to match the code). Born from
`docs-process` after something ships, or drafted by `init` from a scan and marked `status=draft`.

A README in this folder is never a queue entry.
