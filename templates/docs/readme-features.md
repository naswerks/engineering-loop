# features/

What exists — one doc per feature. Descriptive: born from `docs-process` after code ships, verified
against the code, and audited by `docs-audit-feature` (folder mode also flags code with no doc). The
folder IS the feature inventory; there is no separate catalogue.

A README in this folder is never a queue entry.
